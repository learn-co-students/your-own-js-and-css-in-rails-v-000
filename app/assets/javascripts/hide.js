/* function hideWhenClicked(event) {
    var $target = $(event.target);
    $target.hide();
}

 $('#hide_this').click(hideWhenClicked); 
 
 */
 
 
 
 //$('#hide_this').click( e => $(e.target).hide() );
 
 $('#hide_this').click( function(e){ $(e.target).hide() } );